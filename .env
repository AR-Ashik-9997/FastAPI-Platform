# Database Settings (Options: postgresql, mysql, sqlite)
DATABASE_TYPE=sqlite
SQLITE_DB_PATH=./sql_app.db

# PostgreSQL / MySQL Settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=ai_db